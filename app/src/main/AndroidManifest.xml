<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.D308VacationPlan">
        <receiver android:name=".UI.MyVacationReceiver"
            android:enabled="true"
            android:exported="true"></receiver>
        <receiver android:name=".UI.MyExcursionReceiver"
            android:enabled="true"
            android:exported="true"></receiver>
        <activity
            android:name=".UI.ExcursionDetailActivity"
            android:label="Excursion Detail"
            android:parentActivityName=".UI.VacationDetailsActivity"
            android:exported="false" />
        <activity
            android:name=".UI.VacationDetailsActivity"
            android:label="Vacation Detail"
            android:parentActivityName=".UI.VacationListActivity"
            android:exported="false" />
        <activity
            android:name=".UI.VacationListActivity"
            android:label="Vacation List"
            android:parentActivityName=".UI.MainActivity"
            android:exported="false" />
        <activity
            android:name=".UI.MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>